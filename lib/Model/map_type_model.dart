class MapTypeModel {
  bool isSelected;
  final int id;
  final String image;
  final String text;
  final String fileName;

  MapTypeModel(this.id, this.isSelected, this.image, this.text, this.fileName);
}